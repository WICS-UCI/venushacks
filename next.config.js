/** @type {import('next').NextConfig} */
const nextConfig = {
	output: "export",
	reactStrictMode: true,
  // rewrites: async () => ([]),
  // redirects: async () => ([]),
};

module.exports = nextConfig;
